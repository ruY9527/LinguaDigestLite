# 代码优化计划 - 2026-05-03

## 概述

针对 LinguaDigestLite 项目中代码行数超过1000行的文件进行重构优化，目标是将每个文件的行数降低到800行以下。

### 目标文件

| 文件 | 当前行数 | 目标行数 |
|------|----------|----------|
| Services/DatabaseManager.swift | 1012 | <800 |
| Views/SettingsView.swift | 1083 | <800 |
| Services/FeedService.swift | 1249 | <800 |
| Views/VocabularyListView.swift | 1502 | <800 |
| Views/ReaderView.swift | 1685 | <800 |

---

## 1. DatabaseManager.swift (1012行 → 目标<800)

### 问题分析
单一类处理6种实体的CRUD操作：Feeds/Articles/Vocabulary/Categories/Sentences/Logs

### 拆分方案

```
Services/
├── DatabaseManager.swift          # 主管理器（保留通用方法、表创建、迁移逻辑）
├── FeedStorage.swift              # Feed CRUD (~80行)
├── ArticleStorage.swift           # Article CRUD (~150行)
├── VocabularyStorage.swift        # Vocabulary CRUD (~200行)
├── CategoryStorage.swift          # Category CRUD (~80行)
├── SentenceStorage.swift          # SavedSentence CRUD (~100行)
└── RefreshLogStorage.swift        # RefreshLog CRUD (~80行)
```

### 具体拆分内容

| 新文件 | 主要职责 |
|--------|----------|
| DatabaseManager.swift | init、createTables、ensureVocabularyCategoryIdsColumn、sanitizeLiteralNilValues、migrateFromUserDefaults、initializeCategories、Row映射工具方法 |
| FeedStorage.swift | insertFeed、fetchAllFeeds、fetchActiveFeeds、addFeed、updateFeed、deleteFeed、syncBuiltInFeeds、resetBuiltInFeeds、feedFromRow |
| ArticleStorage.swift | insertArticle、fetchAllArticles、fetchArticles、fetchFavoriteArticles、fetchUnreadArticles、addArticle、updateArticle、articleExists、addArticles、markArticleAsRead、toggleArticleFavorite、updateReadingProgress、deleteArticle、cleanupOldArticles、articleFromRow |
| VocabularyStorage.swift | insertVocabulary、fetchAllVocabulary、fetchVocabulary、searchVocabulary、addVocabulary、updateVocabulary、deleteVocabulary、vocabularyFromRow、通知方法 |
| CategoryStorage.swift | insertCategory、fetchAllCategories、addCategory、updateCategory、deleteCategory、categoryFromRow |
| SentenceStorage.swift | insertSentence、fetchAllSentences、fetchSentences、addSentence、updateSentence、deleteSentence、sentenceFromRow、通知方法 |
| RefreshLogStorage.swift | insertRefreshLog、addRefreshLog、fetchAllRefreshLogs、fetchRefreshLogs、latestRefreshLog、clearAllRefreshLogs、refreshLogFromRow |

---

## 2. SettingsView.swift (1083行 → 目标<800)

### 问题分析
包含7个内嵌视图+大量UI代码，词典导入/导出功能混杂

### 拆分方案

```
Views/
├── SettingsView.swift              # 主设置视图 (~350行)
├── FontPickerView.swift            # 字体选择 (~30行)
├── ThemePickerView.swift           # 主题选择 (~40行)
├── VoicePickerView.swift           # 语音选择 (~35行)
├── VocabularyLevelPickerView.swift # 词汇等级选择 (~50行)
└── DictionaryImport/
    ├── DictionaryImportSheet.swift        # 导入弹窗 (~165行)
    ├── DictionaryFormatGuideSheet.swift   # 格式说明 (~135行)
    └── DictionaryExportSheet.swift        # 导出弹窗 (~110行)
```

### 具体拆分内容

| 新文件 | 主要职责 |
|--------|----------|
| SettingsView.swift | 主视图、readingSettingsSection、speechSettingsSection、learningSettingsSection、ecdictDictionarySection、dictionaryImportSection、aboutSection、downloadSampleFile、辅助计算属性 |
| FontPickerView.swift | 字体选择列表 |
| ThemePickerView.swift | 主题选择列表 |
| VoicePickerView.swift | 语音选择列表 |
| VocabularyLevelPickerView.swift | 词汇等级选择列表、levelDescription |
| DictionaryImport/DictionaryImportSheet.swift | 词典导入弹窗、importFile、importModeDescription |
| DictionaryImport/DictionaryFormatGuideSheet.swift | 词典格式说明弹窗、fieldRow |
| DictionaryImport/DictionaryExportSheet.swift | 词典导出弹窗、exportDictionary |

---

## 3. FeedService.swift (1249行 → 目标<800)

### 问题分析
包含RSSParser、内容提取算法、HTML解析、HTML实体解码等混杂功能

### 拆分方案

```
Services/
├── FeedService.swift               # 主服务 (~300行)
├── RSSParser.swift                 # RSS解析器 (~280行)
├── HTMLContentExtractor.swift       # 内容提取 (~400行)
└── HTMLEntityDecoder.swift         # HTML实体解码 (~150行)
```

### 具体拆分内容

| 新文件 | 主要职责 |
|--------|----------|
| FeedService.swift | parseFeed、fetchData、fetchFullArticleContent、extractArticleContent、extractMainContent、chooseBestContent、scoreContentCandidate、normalizeArticleText、extractMetaContent、containsBoilerplate |
| RSSParser.swift | RSSParser类、ArticleBuilder、ParsingState、parse、XMLParserDelegate实现、handleFeedElement、handleItemElement、parseDate |
| HTMLContentExtractor.swift | extractBBCContent、extractBBCDataBlocks、extractGuardianContent、extractNPRContent、extractMITTRContent、extractGenericContent、extractTagContent、extractByClass、findTagEnd、extractParagraphs、extractParagraphsFromContainer、isNavigationOrSidebar、isLikelyArticleParagraph、looksLikeCodeSnippet、cleanHTMLTags |
| HTMLEntityDecoder.swift | decodeHTMLEntities、decodeHexEntities、decodeDecimalEntities、namedEntities字典 |

---

## 4. VocabularyListView.swift (1502行 → 目标<800)

### 问题分析
包含词汇/句子两大模块，10+个内嵌视图，ReviewView复杂

### 拆分方案

```
Views/
├── VocabularyListView.swift         # 主视图 (~460行)
├── SentenceListView.swift           # 句子内容区域 (~150行)
├── VocabularyRowView.swift         # 词汇行视图 (~330行)
├── SentenceRowView.swift           # 句子行视图 (~140行)
├── ReviewView.swift                # 复习视图 (~180行)
├── CategoryViews.swift             # CategoryListView + AddCategoryView (~190行)
└── Components/
    └── StatItem.swift              # 统计项 (~20行)
```

### 具体拆分内容

| 新文件 | 主要职责 |
|--------|----------|
| VocabularyListView.swift | 主视图、vocabularyContent、sentenceContent、contentTypePicker、categoryHeaderView、statisticsCard、vocabularyList、filteredVocabulary、emptyCategoryView、emptyStateView、sentenceCategoryHeaderView、sentenceStatisticsCard、getCategoriesForVocabulary |
| SentenceListView.swift | sentenceContent、sentenceList、filteredSentences、sentenceEmptyStateView、getCategoriesForSentence |
| VocabularyRowView.swift | 词汇行完整视图、headerRow、metadataRow、definitionSection、englishDefinitionSection、exampleSection、reviewSection、speakButton、cardBackground、masteryIndicator、masteryColor |
| SentenceRowView.swift | 句子行完整视图 |
| ReviewView.swift | 复习视图、progressHeader、wordCard、ratingButtons、definitionListView |
| CategoryViews.swift | CategoryListView、AddCategoryView |
| Components/StatItem.swift | StatItem组件 |

---

## 5. ReaderView.swift (1685行 → 目标<800)

### 问题分析
包含UIViewRepresentable、复杂文本处理、内嵌ReaderTextView、SystemDictionarySheetView

### 拆分方案

```
Views/
├── ReaderView.swift                # 主阅读器 (~420行)
├── ReaderTextView.swift            # UITextView子类 (~95行)
├── ArticleReaderTextView.swift     # UIViewRepresentable (~340行)
├── SystemDictionarySheetView.swift # 系统词典弹窗 (~150行)
└── Components/
    └── ReaderMetaPill.swift        # 头部标签 (~20行)
```

### 具体拆分内容

| 新文件 | 主要职责 |
|--------|----------|
| ReaderView.swift | 主视图、mainContent、readingHeroCard、summaryCard、articleBodyCard、articleTitleView、articleHeader、themeBackgroundColor、speakingControlBar、wordFloatingCard、wordHeader、wordDefinitions、categorySelectionAndAdd、handleWordDoubleTap、shareArticle、openInBrowser、copyLink |
| ReaderTextView.swift | ReaderTextView类、didMoveToWindow、canPerformAction、editMenu、translateSelectedText、saveSelectedSentence、clearTextSelection |
| ArticleReaderTextView.swift | UIViewRepresentable、makeUIView、updateUIView、buildFullText、splitIntoParagraphs、paragraphIndexForCharacterOffset、Coordinator类 |
| SystemDictionarySheetView.swift | 系统词典弹窗、quickLookupSummary |
| Components/ReaderMetaPill.swift | ReaderMetaPill组件 |

---

## 实施优先级

| 优先级 | 文件 | 难度 | 预计减少行数 |
|--------|------|------|-------------|
| 1 | SettingsView.swift | 中 | ~450行 |
| 2 | FeedService.swift | 高 | ~500行 |
| 3 | VocabularyListView.swift | 中 | ~650行 |
| 4 | ReaderView.swift | 高 | ~600行 |
| 5 | DatabaseManager.swift | 中 | ~450行 |

---

## 注意事项

1. **保持功能不变**：重构过程中不改变现有功能，仅调整代码组织结构
2. **测试优先**：每个文件重构完成后运行测试确保功能正常
3. **逐步推进**：按优先级顺序进行，每个文件重构完成并测试通过后再进行下一个
4. **文件引用更新**：确保import语句和文件引用正确更新
5. **保留注释**：保留重要的业务逻辑注释和MARK标记